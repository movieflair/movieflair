
// Using the new ListEditor component for custom list management
import ListEditor from './lists/ListEditor';

const CustomListManager = () => {
  return <ListEditor />;
};

export default CustomListManager;
